# Liên kết cùng Supabase project với app user:
#   supabase link --project-ref hptovpbiwvtngorhdhhm
#
# Chỉ deploy các function SAO CHÉP / RIÊNG admin repo từ đây — xem supabase/functions/README.md
#
# P0 CS + dashboard + banner: deploy từ repo Ngay-lanh-thang-tot (canonical, CORS allowlist).

project_id = "link-with-supabase-cli"

[functions.admin-config]
verify_jwt = false

[functions.admin-user-actions]
verify_jwt = false

[functions.admin-dashboard-stats]
verify_jwt = false
