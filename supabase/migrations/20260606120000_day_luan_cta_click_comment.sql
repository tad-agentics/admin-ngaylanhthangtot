-- Metric semantics: count red CTA "Hỏi tiếp về ngày này" clicks, not ask submits.
comment on column public.profiles.day_luan_follow_up_click_count is
  'Số lần user bấm CTA "Hỏi tiếp về ngày này" (lifetime).';
